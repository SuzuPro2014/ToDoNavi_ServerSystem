<% @page_title= "新規位置情報登録" %>
<h1><%= @page_title %></h1>

<%= form_for [:admin, @locationinfo] do |form| %>
<%= render "form", form: form %>
<table class="tcont">
  <tr>
    <td><%= form.submit %></td>
  </tr>
</table>
<% end %>

<table class="tcont">
  <tr>
    <td><%= link_to "一覧に戻る", :admin_locationinfos %></td>
  </tr>
</table>
